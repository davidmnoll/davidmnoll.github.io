const FosScopeBuilder = () => (
  <div className="rounded-3xl border border-white/10 bg-slate-900/50 p-6 text-sm text-white">
    <p className="mb-2 text-xs uppercase tracking-[0.4em] text-foreground/60">FOS Scope Builder</p>
    <p>
      The in-progress scope builder UI currently lives in <code>fosScopeBuilder.wip.tsx</code>. Swap it back in when
      you are ready to continue iterating.
    </p>
  </div>
);

export default FosScopeBuilder;
